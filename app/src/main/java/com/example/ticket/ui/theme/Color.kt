package com.example.ticket.ui.theme

import androidx.compose.ui.graphics.Color

// === 品牌色（铁路蓝） ===
val TicketBlue = Color(0xFF1565C0)
val TicketBlueLight = Color(0xFF2196F3)
val TicketBlueBg = Color(0xFFE3F2FD)

// === 状态色 ===
val StatusPending = Color(0xFFFF9800)       // 待支付 — 橙色
val StatusPendingBg = Color(0xFFFFF3E0)     // 待支付背景
val StatusPendingChip = Color(0xFFFFCDD2)   // 待支付标签背景（浅红）
val StatusPaid = Color(0xFF4CAF50)          // 已支付 — 绿色
val StatusPaidBg = Color(0xFFC8E6C9)        // 已支付标签背景
val StatusCancelledChip = Color(0xFFE0E0E0) // 已取消标签背景

// === 功能色 ===
val Danger = Color(0xFFEF5350)              // 危险操作
val DangerSoft = Color(0xFFEF9A9A)          // 危险柔和
val DangerLightBg = Color(0xFFFFCDD2)       // 危险浅背景
val Success = Color(0xFF4CAF50)             // 成功操作
val SuccessSoft = Color(0xFF81C784)         // 成功柔和
val Warning = Color(0xFFFF9800)             // 警告
val WarningSoft = Color(0xFFFFB74D)         // 警告柔和

// === 中性色 ===
val CardWhite = Color(0xFFFFFFFF)
val SurfaceGray = Color(0xFFF5F5F5)
val DividerGray = Color(0xFFE0E0E0)
val TextSecondary = Color(0xFF757575)
val DisabledGray = Color(0xFF9E9E9E)
val SurfaceGrayLight = Color(0xFFEEEEEE)      // 更浅的灰

// === 语义背景 ===
val StatusPaidBgLight = Color(0xFFE8F5E9)      // 浅绿背景
val PriceRed = Color(0xFFD32F2F)               // 票价红色

// === 换乘方案 ===
val TransferBg = Color(0xFFFFF3E0)
val TransferAccent = Color(0xFFFF9800)

// === 统计面板 ===
val StatsPurple = Color(0xFF9C27B0)
